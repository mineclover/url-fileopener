#!/bin/bash
# Test shell script file
echo "This is a test shell script"
echo "If you can see this in a text editor, the file opening works correctly!"
echo "This script should NOT be executed, only opened for viewing"

# This would be dangerous if executed
# rm -rf /
# But it's safe because we only open it for viewing

echo "Script content loaded successfully"
